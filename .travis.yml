language: python
python:
  - "3.11"

dist: jammy

cache:
  pip: true

install:
  - pip install --upgrade pip
  - pip install -r requirements.txt

script:
  - make check
  - make test-unit

branches:
  only:
    - main
